{
  "name": "soundboard",
  "version": "1.0.0",
  "description": "soundboard that just works",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron src/",
    "build" : "electron-packager . soundboard --platform=win32 --arch=x64 --overwrite --out=dist/"
  },
  "author": "valium007",
  "license": "MIT",
  "devDependencies": {
    "@electron/remote": "^2.1.1",
    "remote": "^0.2.6",
    "@electron/packager": "^18.1.3",
    "electron": "^28.0.0"
  }
}
